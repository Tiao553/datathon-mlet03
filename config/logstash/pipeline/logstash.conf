input {
  tcp {
    port => 50000
    codec => json_lines
  }
}

filter {
  # Add processing/parsing here if needed
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "app-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
